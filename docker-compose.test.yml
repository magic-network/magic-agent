version: '3'

services:
  sut:
    depends_on:
      - lint
      - test

  lint:
    build: 
      context: .
      dockerfile: Dockerfile.agent
    command: pylint -rn --errors-only ./magic

  test:
    build: 
      context: .
      dockerfile: Dockerfile.agent
    command: pytest
